# 1. 忽略所有以点开头的文件和文件夹 (.env, .vscode 等)
.*

# 2. 忽略所有以下划线开头的文件和文件夹 (_backup, _temp 等)
_*

# -----------------------------------------------------------
# 【例外规则 / 白名单】(使用 ! 来重新追踪被上面规则忽略的文件)
# -----------------------------------------------------------

# 保留 .gitignore 文件本身 (否则会被 .* 忽略)
!.gitignore

# 保留 Python 包的初始化文件 (否则会被 _* 忽略)
!__init__.py

# 本地 Maya/Qt 视觉验证产物
tests/artifacts/
